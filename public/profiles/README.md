# Executive profile photos

Place image files here (`.jpg`, `.png`, `.webp`, etc.) and set the **filename only** on each persona in Admin → Personas (e.g. `ceo.jpg`).

They are served at `/profiles/{filename}` (e.g. `http://localhost:3000/profiles/ceo.jpg`).
