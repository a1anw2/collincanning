/** Onboarding prompt for agents joining mid-simulation. */

export const onboardingPrompt = `Summarize the executive channel discussion so far for a new participant joining mid-conversation.
Include: main topic, key positions of each role, open questions, and recent developments.
Be concise (under 300 words). Plain text only, no JSON.`;
